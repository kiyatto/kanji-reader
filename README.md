Webcam-based interactive image classification for 3036 kanji + hiragana characters (handwritten) using ETLG9 data

To run this program, you need PyTorch installed. Then do `pip install -r requirements.txt` and `./setup.sh` which install necessary dependencies and Google's hand landmarker model respectively.

Then to run the script, do `python script.py`.